spring:
  config:
    activate:
      on-profile: prod
  jpa:
    show-sql: false
  datasource:
    url: jdbc:mysql://localhost:3306/prod
  liquibase:
    enabled: false


logging:
  level:
    root: info
